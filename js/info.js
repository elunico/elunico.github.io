window.onload = () => {
  run_common();

  typeText('Information &amp; Contact', '#mainHeader', () => {
    fade();
  });
};



// TODO: why does this need timeout
// setTimeout(() => buttonFade(), 60);
